{
  "name": "dressup-strategy",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "cn-search"
  ],
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "lint": "eslint eslint.config.mjs src/domain \"scripts/*.mjs\" tests cn-search/scripts cn-search/src tool.js --max-warnings 0",
    "test": "node --test tests/*.test.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "check": "npm run lint && npm test && npm run validate:data",
    "build:cn-index": "npm run build:cn-index --workspace cn-search"
  },
  "devDependencies": {
    "eslint": "10.0.0"
  }
}
